/** Player collision half-width, in world units. */
export const PLAYER_HALF_WIDTH = 250
/** Collision half-width for a hazard, before its visual scale is applied. */
export const OBSTACLE_HALF_WIDTH = 240
export const COIN_PICKUP_RADIUS = 380
/** Clearance needed to fly over a hazard rather than hit it. */
export const JUMP_CLEARANCE = 330
/** Ramp launches are stronger than manual jumps but capped to keep landings readable. */
export const RAMP_MIN_LAUNCH_SCALE = 1.05
export const RAMP_MAX_LAUNCH_SCALE = 1.45
